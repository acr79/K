# Core
openai>=1.30
qdrant-client>=1.9
sentence-transformers>=3.0
pyyaml>=6.0

# Web research
duckduckgo-search>=6.0
# tavily-python>=0.3        # optional upgrade — set TAVILY_API_KEY for better results

# Voice (macOS — install separately if needed)
# openai-whisper>=20240930
# sounddevice>=0.4
# soundfile>=0.12

# Optional TTS fallback (non-macOS)
# pyttsx3>=2.90
